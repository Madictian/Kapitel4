/* import java.util.Scanner;

public class Opg4_3 {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.println("savannah: ");
        double savannah = input.nextDouble();
        System.out.println("charlotte: ");
        double charlotte = input.nextDouble();
        System.out.println("atlanta: ");
        double atlanta = input.nextDouble();
        System.out.println("orlando: ");
        double orlando = input.nextDouble();
        /*
        atlanta : 28.54, -81,38
        orlando : 33.75, 84.39
        savannah: 32.08, 81.09
        charlotte: 35.23, 80.84
         */                                /*
        double x = input.nextDouble();
        double y = input.nextDouble();
        double x2 = input.nextDouble();
        double y2 = input.nextDouble();
        double x3 = input.nextDouble();
        double y3 = input.nextDouble();
        double x4 = input.nextDouble();
        double y4 = input.nextDouble();

        double s1 = ((x + y) + (x2 + y2) + (x3 + y3)) / 2;
        double s2 = ((x + y) + (x2 + y2) + (x4 + y4)) / 2;

        double a = Math.sqrt((x2 - x3) * (x2 - x3) * (y2 - y3) * (y2 - y3));
        double b = Math.sqrt((x - x3) * (x - x3) * (y - y3) * (y - y3));
        double c = Math.sqrt((x - x2) * (x - x2) * (y - y2) * (y - y2));

        double a2 = Math.sqrt((x2 - x4) * (x2 - x4) * (y2 - y4) * (y2 - y4));
        double b2 = Math.sqrt((x - x4) * (x - x4) * (y - y4) * (y - y4));
        double c2 = Math.sqrt((x - x2) * (x - x2) * (y - y2) * (y - y2));

        double A = Math.toDegrees(Math.acos((a * a - b * b - c * c) / (-2 * b * c)));
        double B = Math.toDegrees(Math.acos((b * b - a * a - c * c) / (-2 * a * c)));
        double C = Math.toDegrees(Math.acos((c * c - b * b - a * a) / (-2 * a * b)));

        double A2 = Math.toDegrees(Math.acos((a * a - b * b - c2 * c2) / (-2 * b * c2)));
        double B2 = Math.toDegrees(Math.acos((b * b - a * a - c2 * c2) / (-2 * a * c2)));
        double C2 = Math.toDegrees(Math.acos((c2 * c2 - b * b - a * a) / (-2 * a * b)));


    }
}
*/